# Phase 3 — THE CORRECTION: Level-0 scorecard

Adjudication artifact, 2026-08-12. Evidence level 0 (prose/spec) — **capped at
55/100** by the master brief regardless of raw score. Scale 0–5 per criterion;
no 5s from prose. Scored honestly against the brief's interpretation notes
(§65), with repo facts from `00-repository-truth.md` cited where they ground a
score.

Scope note: per founder instruction this is a single-candidate run. The other
four candidates are unscored and on the shelf. The brief's warning against
detail-asymmetry bias is moot for ranking but noted: this score cannot be
compared against another candidate until that candidate gets the same depth.

| Criterion | W | Score | Reason |
|---|---:|---:|---|
| Philosophical fidelity | 14 | 4 | State, authority, consequence are literal mechanics; the band is *derived from a record* (`u*`), so representation-acting-on-reality is the core loop, and `V = D × C` survives verbatim. |
| Menace through comprehension | 10 | 4 | "Calm = enforcement already won" retroactively poisons the opening exactly as the law requires. Knowing the rule makes the beauty worse. |
| Runtime causal legibility | 10 | **2** | The whole native risk lives here: computed enforcement may read as snap/glitch. Nothing on paper can move this. **Primary kill-test target.** |
| System over authored-form dependence | 9 | 4 | Core value is provable on a schematic seeded graph; no perfect asset, no bake, no environment. Strong §38 alignment (constraint + propagation). |
| First-frame visual authority | 8 | **2** | Unknowable from prose by the brief's own definition; every dead direction promised a divine opening on paper too. Conservative until a still exists. |
| Reinterpretation/inversion strength | 8 | 4 | Later evidence (one enforcement event) changes the meaning of an earlier state (all pre-existing calm) with zero visual change — the strongest inversion class available. |
| Reuse of proven repository systems | 7 | 4 | KNOWN reuse: CausalPulseSimulation, Worker/Client, fixed-step, checksum, validate/calibrate/bench harness, isolated-entry convention. Graph is injectable by design. |
| Art-production risk | 7 | 4 | No bake, no organic asset, no set pieces. The risk is legibility (scored above), not production. |
| Technical implementation risk | 6 | 4 | Clear state architecture on a proven engine; new code is a bounded correction pass + small graph synth. CPU-debuggable, no framework needs. |
| Mobile preservation | 5 | 3 | Touch injection is native to the mechanic; narrow-screen composition undesigned. Credible, unproven. |
| Reduced-motion preservation | 4 | **2** | Its own §58 kill condition ("unexplained before/after") applies; the strain-triptych answer is designed but untested. |
| Performance feasibility | 4 | 3 | Prior graph ran ~6.2k nodes fixed-step in a Worker with bench tooling; correction pass is O(nodes)·K. Credible; not yet measured on this workload. |
| Editorial/company handoff | 3 | 4 | `ADJUSTMENTS APPLIED: N` → "the file is an instruction" → Desk42/Brawler mapping is direct; the count is real, so the handoff is factual. |
| Dark Lattice specificity | 3 | 3 | Enforcement-as-hero with record-derived authority is specific; constraint-projection *visuals* exist as a genre, so not maximal. |
| Kill-test reversibility/cost | 2 | 4 | One isolated entry + branch, no production files, one capture session; removable without unpicking anything. |

**Raw:** 344/500 → **68.8/100**
**Evidence cap (Level 0):** **55/100**
**Capped score: 55/100**
**Hard blockers:** none failed; three GUARDED (duality-without-caption, mobile,
reduced motion) — see dossier blocker table.
**Cheapest unresolved test:** the one-capture enforcement A/B
(`04-correction-kill-test.md`).
**Status: BUILD KILL TEST.**

## What the kill test must move

The three scores holding this candidate down are precisely the three things
paper cannot establish:

1. **Causal legibility (2/5, weight 10)** — does enforcement read as
   discipline, blind, with no caption? This is the kill-or-live question.
2. **First-frame authority (2/5, weight 8)** — the OPEN still falls out of the
   same capture session for free.
3. **Reduced motion (2/5, weight 4)** — the strain triptych is composable from
   the same deterministic run's frames.

If the capture session moves those three to ≥3 with real evidence, the
Level-1/2 recomputation clears 70+ before any polish. If the correction
instant reads as snap or glitch after the single permitted parameter-rescue
pass, the candidate dies at the cost of one spike page and one session — which
is the entire point of the harness.
